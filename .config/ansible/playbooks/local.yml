#!/usr/bin/ansible-playbook -i hosts.ini
---
- name: "Local"
  hosts: local
  become: true
  gather_facts: false

  roles:
    - role: roles/systemd-syncthing
      become: true
      tags: syncthing
